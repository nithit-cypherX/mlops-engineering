# Runtime dependencies for the registered Lab 2 model and the Lab 3 API.
# Keep the model/runtime versions from Lab 2; do not upgrade them with the starter.
# Compile for Python 3.11/Linux with hashes into requirements-serve.txt.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
scipy==1.17.1
joblib==1.4.2
cloudpickle==3.1.2
mlflow==2.22.5

# Existing Azure adapter and MLflow access to the registered model.
azure-identity==1.25.3
azure-storage-blob==12.27.1
azureml-mlflow==1.62.0.post6
